pipeline {
    agent any

    stages {
        stage('Deploy') {
            agent{

                docker {
                    image 'node:18-alpine'
                    reuseNode true
                }
            }
           \* steps {
                sh '''
                ls -la
                node --version
                echo 'Hello World'
                npm ci
                npm run build
                echo 'show'
            
                '''
            }*\
            steps {
                sh '''
                ls -la
                npm insta;; netlify-cli -g
                netlify --version
                '''
            }
        }
    }
}