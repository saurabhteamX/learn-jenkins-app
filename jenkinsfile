pipeline {
    agent any

    stages {
        stage('Deploy') {
            agent{

                docker {
                    image 'node:18-alpine'
                    reuseNode true
                }
            }
           /* steps {
                sh '''
                ls -la
                node --version
                echo 'Hello World'
                npm ci
                npm run build
                echo 'show'
            
                '''
            }*/
            steps {
                
                sh '''
                ls -la
                npm install
                npm run build
                sleep 10
                npm install netlify-cli@20.1.1
                node_module/.bin/netlify --version
                npm audit fix --force
                '''
            }
        }
    }
}